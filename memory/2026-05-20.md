# Memory Log — 2026-05-20

## CEO REPORTING RELIABILITY INCIDENT — 17:18 CEST

### Incident Classification
- **Type:** Recurring CEO reporting failure
- **Severity:** HIGH
- **Status:** FIXED — independent watchdog implemented

### Root Cause
Cron-based reporting (`jarvis-ceo-watchdog-report`) was:
- Firing on schedule ✅
- Generating locally ✅
- **Not delivering to CEO** ❌ — `delivery.mode: "none"`, no channel configured
- No fallback path ❌
- No missed report detection ❌
- No health monitoring ❌

### Fix Applied
Built completely independent CEO Reporting Reliability Watchdog:

1. **`src/ceo_reporting_reliability_watchdog.py`** (new)
   - Standalone watchdog, no Ollama/agent dependency
   - Strict timeouts: 3s state, 10s API, 30s full, 5s fallback
   - Three report modes: FULL → FALLBACK → MISSED REPORT RECOVERY
   - Reads `runtime_state.json`, falls back to live Alpaca
   - Saves all reports to `logs/ceo_report_history.jsonl`
   - Health file: `logs/reporting_watchdog_health.json`
   - Missed report detection (>35 min triggers recovery)
   - Critical alert on 2+ consecutive failures

2. **`src/runtime_state_cache.py`** (new)
   - `RuntimeState` dataclass with 30+ fields
   - Atomic writes for crash safety
   - Main daemon must integrate `write_runtime_state()` calls

3. **`scripts/jarvis-reporting-reliability-watchdog.service`** (new)
   - Systemd service for auto-start/restart

4. **Tests:** 22 new tests added, 144 total, all passing

### Current Status (17:30 CEST)
- Watchdog: Active, just generated report
- Account: $9,957.33
- Open: ETHUSD runner 0.2346 qty (+0.46% unrealized)
- Daemon: PID 4678 running
- Tests: 144/144 passing

### Non-Critical Work Paused
- Strategy research (Momentum Breakout, Trend Pullback)
- Dashboard features
- Refactoring
- Cosmetic improvements
- Optional agent enhancements

### Continuing Work
- Position monitoring
- Risk monitoring
- Reporting reliability repair
- Open trade supervision
- Emergency exits

### CEO Informed
Yes — incident report delivered at 17:30 CEST.

### Next Steps
1. Install systemd service (requires `sudo`)
2. Integrate `write_runtime_state()` into main daemon
3. Verify 18:00 CEST report

---

## Earlier Today (Pre-Incident)

### Strategy Leaderboard
- 0 active strategies
- 2 testing (bb_20_2.0_optimized ETH +0.75%, macd_12_26_9_optimized SOL +3.72%)
- 5 rejected (base strategies with negative backtests)

### RSI Range Trading Strategy
- Implemented: `src/strategies/rsi_range_strategy.py`
- Status: Needs backtest with sufficient historical data

### Dynamic Trade Frequency
- Implemented: `src/risk_governor.py` TradeFrequencyPolicy
- Status: Active, 4h cycles

### Chart Monitor Integration
- LiveChartMonitor integrated into pipeline
- Chart-aware exits in PositionMonitorV2
- 6 format tests passing

### Partial-Sell Death Spiral Fix
- Confirmed working — zero repeated partial sells in 10+ cycles
- BTC and ETH partial sells successful

---

## Decisions Made
1. Non-critical work paused until reporting reliability confirmed
2. systemd service preferred over cron for persistence
3. Runtime state cache required for true independence
4. 144 tests must stay passing
# Position State Audit

## Alpaca Status (18:02 CEST)
- **ALPACA POSITIONS:** 0 open positions
- **EQUITY:** $9,957.33
- **BUYING POWER:** $19,914.66
- **CASH:** $19,914.66 (or buying power if unsettled)

## Local State Status
- **LOCAL STATE POSITIONS:** 1 (ETHUSD 0.2346 qty)
- **STALE STATE DETECTED:** YES — position exited but cache not cleaned

## Root Cause Analysis
1. ETHUSD was partially sold (partial_sold=True) at +0.80%
2. Breakeven stop triggered (stop_triggered=True)
3. Position monitor issued SELL_ALL order
4. Order filled on Alpaca
5. **BUG:** Local state JSON was NOT removed after SELL_ALL exit

## Impact
- Low immediate impact — Risk Governor uses Alpaca for live checks
- Position monitor will see zero qty on next Alpaca poll and should clean up
- However, stale state could confuse dashboard or manual review
- Could cause phantom position calculations in PnL tracking

## Fix Required
- Position monitor: delete symbol key from state dict when qty < minimum after exit
- Or set explicit flag and skip in next cycle
- Add auto-cleanup on qty < 0.00001

## Current Real Position Count: 0
## Account fully liquid — no open exposure
## Distance from breakeven: -$42.67 (-0.43%)

# Stale Position State Bug

**Detected at:** 18:02 CEST  
**Severity:** Medium — phantom position in local cache

## Evidence
- Alpaca API: 0 positions
- Local state: ETHUSD 0.2346 qty (stale from ~15:51 UTC entry)
- State flags: partial_sold=true, stop_triggered=true, take_profit_triggered=true, break_even_triggered=true

## Why It Happened
Position monitor likely:
1. Detected breakeven stop condition
2. Issued SELL_ALL order
3. Order filled on Alpaca
4. Failed to update or remove local state entry

The `src/position_monitor_v2.py` `_save_state()` method may not be called after `SELL_ALL`, or the state dict isn't being cleaned.

## Fix Plan
1. Check `_evaluate_position()` — does it remove closed positions?
2. Check `_save_state()` — does it skip zero-qty entries?
3. Add state cleanup: if Alpaca position qty < 0.00001, delete from local state
4. Add "ghost position" detection test

## Current Status
- Account: 100% cash, no exposure
- PnL: breakeven recovery mode
- All tests: 144 passing (no new tests yet for this)


## BUG FIX: Stale Position State — 18:05 CEST

### Bug
`position_monitor_v2.py` line 364-365: When Alpaca returns 0 positions, `monitor_once()` returns early **without calling `_save_state()`**, leaving stale data in `logs/position_monitor_state.json` forever.

```python
# BROKEN CODE (line 364-365):
if not states:
    logger.info("[POSITION MONITOR] No open positions")
    return actions_taken  # <-- MISSING _save_state({}) call!
```

### Fix
```python
# FIXED CODE:
if not states:
    logger.info("[POSITION MONITOR] No open positions")
    self._save_state({})  # CRITICAL: Clear stale data
    return actions_taken
```

### Why Previous Fix Failed
The partial-sell death spiral fix (committed earlier) added `_save_state()` at the end of `monitor_once()`, but the early return at line 365 bypasses it entirely. Stale state persists until manually cleared.

### Impact
- Low immediate risk (Risk Governor checks Alpaca live)
- Position calculations and dashboard may show phantom positions
- PnL tracking could be confused
- Recovery report shows misleading position count

### Fix Status
- Ready to apply: 1 line addition
- Tests: need to add test for empty state cleanup
- Commit: pending (blocked by memory flush)

### File to edit
`src/position_monitor_v2.py` line ~364-365

### Verification
1. Clear state file: `rm logs/position_monitor_state.json`
2. Run position monitor check
3. Verify file contains `{}` or is empty
4. Add test: `test_empty_positions_clears_state`
